import { Router } from "express";

export class AppRoutes {

    static get routes(): Router {

        const router = Router();
    
        //todo
        // router.use('/api/',  );

        return router;
    };
};