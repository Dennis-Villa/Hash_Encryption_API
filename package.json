{
  "name": "hash_encryption_api",
  "version": "1.0.0",
  "description": "An API to handle the detection, encryption and decryption of messages and files using various algorithms.",
  "main": "app.js",
  "scripts": {
    "dev": "tsnd --respawn --clear src/app.ts",
    "build": "rimraf ./dist && tsc",
    "build:install": "npm install && npm run build",
    "start": "node dist/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Dennis Villavicencio",
  "license": "ISC",
  "devDependencies": {
    "@types/archiver": "^6.0.2",
    "@types/express": "^5.0.0",
    "@types/express-fileupload": "^1.5.1",
    "@types/node": "^22.7.5",
    "rimraf": "^6.0.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "archiver": "^7.0.1",
    "dotenv": "^16.4.5",
    "env-var": "^7.5.0",
    "express": "^4.21.1",
    "express-fileupload": "^1.5.1",
    "jszip": "^3.10.1"
  }
}
